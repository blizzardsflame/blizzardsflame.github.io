var options = {
            target: '#info',
            url: 'processauto.php',
            success: function(){
                alert('Votre demande a bien été transmise')
            },
            clearForm: true
        }

    //$('#autoform').ajaxForm(options)
    $('#autoform').submit(function(){
        $(this).ajaxSubmit(sendifValid);
        return false
    })